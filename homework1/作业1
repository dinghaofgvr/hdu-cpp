# 提示用户输入两个整数
num1 = int(input("请输入第一个整数："))
num2 = int(input("请输入第二个整数："))

# 提示用户选择操作
operation = int(input("请选择操作（1:加法，2:减法）："))

# 根据用户的选择执行相应的操作
if operation == 1:
    结果=1+2
    print(f"结果：{num1} + {num2} = {result}")
elif operation == 2:
    结果=1-2
    print(f"结果：{num1} - {num2} = {result}")
其他:
    印刷品 ( "无效的操作选择，请输入 1 或 2。" )
